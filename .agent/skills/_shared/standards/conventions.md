# 📄 格式标准说明

## Skill 引用规范

所有 Skill 应遵循以下目录结构和命名规范。

### 目录结构

### 目录结构

```
.agent/skills/
├── _claude_global/ → ~/.claude/skills  # 外部引用
├── _shared/                            # 全局共享
└── project/                            # 项目专属分类
    └── [skill-name]/                   # 具体 Skill
```

### 文件引用

在各个 SKILL.md 中引用资源时（假设 Skill 在 `project/[skill-name]/` 下）：

1. **项目内资源**: `resources/xxx.md`
2. **全局共享资源**: `../../_shared/standards/xxx.md`
3. **外部引用资源**: `../../_claude_global/frontend-design/SKILL.md`

## 编写规范

- 使用 Markdown 格式
- 步骤清晰，使用编号列表
- 关键风险操作使用 Alert 提示（> [!WARNING]）
