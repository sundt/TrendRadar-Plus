---
name: cleanup-temp-files
description: 清理项目中遗留的临时测试文件
---

# 清理临时测试文件

## 概述
开发过程中经常创建临时测试脚本（`debug_*.py`, `test_*.py`, `scrape_*.py`），但容易忘记删除。本 Skill 帮助定期清理这些遗留文件。

## 🚨 为什么需要清理

| 问题 | 影响 |
|------|------|
| 代码库混乱 | 难以区分生产代码和测试代码 |
| Git 污染 | 容易误提交临时文件 |
| 磁盘占用 | 无用文件累积 |
| 安全风险 | 测试代码可能包含敏感信息 |

---

## 🧹 清理步骤

// turbo-all

### 1. 自动检测并清理（推荐）

```bash
cd /Users/sun/Downloads/hotnews
./scripts/cleanup_temp_scripts.sh
```

**会检测的文件类型**：
- `debug_*.py` - 调试脚本
- `test_*.py` - 单元测试或临时测试
- `scrape_*.py` - 临时爬虫脚本
- `temp_*.py` - 明确标记的临时文件

### 2. 手动检查

```bash
# 列出所有可疑的临时文件
find . -maxdepth 1 -type f \( -name "debug_*" -o -name "test_*" -o -name "scrape_*" -o -name "temp_*" \)

# 查看文件详情（大小、修改时间）
ls -lh debug_* test_* scrape_* temp_* 2>/dev/null
```

### 3. 选择性删除

```bash
# 删除特定类型
rm debug_*.py
rm test_*.py

# 或一次性全部删除
rm debug_* test_* scrape_* temp_*
```

---

## 💡 最佳实践

### ✅ 避免创建临时文件

1. **直接在目标位置测试**
   - 爬虫测试：Admin 后台 → 自定义源管理 → 测试运行
   - Python 代码：使用 Python REPL 或 Jupyter

2. **使用临时目录**
   ```bash
   # 在 /tmp 目录创建测试文件
   vim /tmp/test_something.py
   python /tmp/test_something.py
   ```

3. **使用一致的命名前缀**
   - `temp_` - 明确标记为临时文件
   - `debug_` - 调试用途
   - `test_` - 测试脚本

### ⚠️ 提交前检查

在提交代码前，务必清理临时文件：

```bash
# 1. 清理临时文件
./scripts/cleanup_temp_scripts.sh

# 2. 查看 Git 状态
git status

# 3. 确保没有临时文件被跟踪
git ls-files | grep -E "debug_|test_|scrape_|temp_"
```

---

## 📋 定期清理建议

| 时机 | 建议操作 |
|------|----------|
| **每次开发结束** | 运行清理脚本 |
| **提交代码前** | 检查并清理 |
| **每周** | 手动检查一次 |
| **部署前** | 强制清理 |

---

## 🔧 自定义清理规则

如果需要清理其他类型的文件，编辑清理脚本：

```bash
# 编辑脚本
vim scripts/cleanup_temp_scripts.sh

# 修改检测模式（第 12 行）
TEMP_FILES=$(find . -maxdepth 1 -type f \( -name "debug_*" -o -name "test_*" -o -name "你的模式*" \))
```

---

## 📚 相关资源

- [清理脚本源码](../../scripts/cleanup_temp_scripts.sh)
- [Git 忽略规则](.gitignore) - 防止临时文件被提交
